package com.plutus.ds.maker;

import com.plutus.ds.entities.Exchange;

public class ExchangeMaker {
    public Exchange make(String exchangeString){
        Exchange exchange = new Exchange();

        // 한줄을 읽어온다.
        // ,로 자른다
        // 0번은 localDateTime으로 바꾼다.
        // 1번, 2번은 넣는다.


        return exchange;
    }
}
